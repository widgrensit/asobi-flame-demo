name: asobi_flame_demo
description: "Flame arena demo for the Asobi game backend"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.22.0
  flame_asobi:
    git:
      url: https://github.com/widgrensit/flame_asobi.git
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
